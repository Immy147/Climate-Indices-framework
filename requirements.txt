# Core
xarray>=2024.1.0
dask[distributed]>=2024.1.0
netCDF4>=1.6.0
numpy>=1.24.0
pandas>=2.0.0
scipy>=1.10.0
bottleneck>=1.3.0
cftime>=1.6.0

# Climate indices
xclim>=0.48.0

# Trend statistics
pymannkendall>=1.4.0

# Plotting
matplotlib>=3.7.0

# Export
openpyxl>=3.1.0

# Optional: GeoTIFF export (climate_indices.export.to_geotiff).
# The rest of the package works fine without this -- it's only imported
# lazily inside to_geotiff(), with a clear error message if missing.
rioxarray>=0.15.0

# Optional: only needed if you later add shapefile/GeoPackage clipping
# geopandas>=0.14.0
# shapely>=2.0.0
